# 🏛️ Arquitectura FidelizaCRM - Diagrama Visual

## Vista General de Capas

```
┌─────────────────────────────────────────────────────────────────┐
│                        PRESENTACIÓN                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ React Pages  │  │  Components  │  │   Dialogs    │          │
│  │  (Next.js)   │  │    (UI)      │  │   (Forms)    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                  │                   │
│         └─────────────────┴──────────────────┘                   │
│                           │                                      │
└───────────────────────────┼──────────────────────────────────────┘
                            │ Llaman a
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ORQUESTACIÓN (Server Actions)                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/actions/                                            │   │
│  │  • clientes.ts  → createCliente(), updateCliente()      │   │
│  │  • users.ts     → createUser(), deleteUser()            │   │
│  │  • citas.ts     → createCita(), cancelCita()            │   │
│  │  • servicios.ts, plantillas.ts, recordatorios.ts        │   │
│  └──────┬───────────────────────────────────────────────────┘   │
│         │ 1. Autenticación  2. Servicio  3. Cache  4. Return    │
└─────────┼─────────────────────────────────────────────────────┘
          │
          ├─────────────┐
          │             │
          ▼             ▼
┌──────────────┐  ┌─────────────────────────────────────────┐
│     AUTH     │  │         LÓGICA DE NEGOCIO               │
│ Middleware   │  │  ┌────────────────────────────────────┐ │
│              │  │  │  lib/services/                     │ │
│ • withAuth   │  │  │  • ClienteService                  │ │
│ • requireAdmin│ │  │    - Validaciones                  │ │
│ • requireUser│  │  │    - Reglas de negocio             │ │
│              │  │  │    - Cálculos complejos            │ │
│              │  │  │  • UserService, CitaService, etc.  │ │
│              │  │  └───────────┬────────────────────────┘ │
└──────────────┘  └──────────────┼──────────────────────────┘
                                 │ Usa
                                 ▼
                  ┌─────────────────────────────────────────┐
                  │       ACCESO A DATOS                    │
                  │  ┌────────────────────────────────────┐ │
                  │  │  lib/repositories/                 │ │
                  │  │  • BaseRepository<T>               │ │
                  │  │    - findAll(), findById()         │ │
                  │  │    - create(), update(), delete()  │ │
                  │  │  • ClienteRepository               │ │
                  │  │  • CitaRepository                  │ │
                  │  │  • ProfileRepository, etc.         │ │
                  │  └───────────┬────────────────────────┘ │
                  └──────────────┼──────────────────────────┘
                                 │ Consulta
                                 ▼
                  ┌─────────────────────────────────────────┐
                  │         BASE DE DATOS                   │
                  │  ┌────────────────────────────────────┐ │
                  │  │  Supabase PostgreSQL               │ │
                  │  │  • clientes                        │ │
                  │  │  • citas                           │ │
                  │  │  • servicios, subservicios         │ │
                  │  │  • plantillas_mensaje              │ │
                  │  │  • recordatorios                   │ │
                  │  │  • profiles                        │ │
                  │  └────────────────────────────────────┘ │
                  └─────────────────────────────────────────┘

        ┌────────────────────────────────────────────────┐
        │  UTILIDADES COMPARTIDAS (Cross-cutting)        │
        │  ┌──────────────┐  ┌──────────────────────┐   │
        │  │   Errors     │  │   Utils              │   │
        │  │ • AppError   │  │ • formatPrice()      │   │
        │  │ • handleError│  │ • formatDate()       │   │
        │  │              │  │ • isValidEmail()     │   │
        │  └──────────────┘  └──────────────────────┘   │
        └────────────────────────────────────────────────┘
```

---

## Flujo de una Petición: CREATE CLIENTE

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. USUARIO                                                       │
│    Click en "Crear Cliente" en UI                               │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. COMPONENTE: CreateClienteDialog                              │
│    Recolecta datos del formulario                               │
│    { nombre: "Juan", contacto: "0981234567", ... }              │
└────────────┬────────────────────────────────────────────────────┘
             │ Llama a action
             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. ACTION: createCliente(data)                                  │
│    try {                                                         │
│      const { supabase } = await withAuth() ──┐                  │
│                                              │                  │
└──────────────────────────────────────────────┼──────────────────┘
                                               │
                                               ▼
                              ┌────────────────────────────────┐
                              │ AUTH MIDDLEWARE                │
                              │ • Obtener usuario autenticado  │
                              │ • Validar sesión activa        │
                              │ • Retornar { supabase, user }  │
                              └────────────┬───────────────────┘
                                           │ Autenticado ✓
┌──────────────────────────────────────────┼───────────────────────┐
│ 3. ACTION (continuación)                 │                       │
│      const repo = new ClienteRepository(supabase)                │
│      const service = new ClienteService(repo)                    │
│      const cliente = await service.create(data) ──┐              │
│                                                    │              │
└────────────────────────────────────────────────────┼──────────────┘
                                                     │
                                                     ▼
                              ┌──────────────────────────────────┐
                              │ SERVICE: ClienteService.create() │
                              │ 1. Validar datos                 │
                              │    ✓ Nombre requerido            │
                              │    ✓ Email válido                │
                              │    ✓ Contacto formato correcto   │
                              │ 2. Reglas de negocio             │
                              │    ✓ Si es_menor → responsable   │
                              │    ✓ Contacto único              │
                              │ 3. Llamar repositorio ─────┐     │
                              └────────────────────────────┼─────┘
                                                           │
                                                           ▼
                              ┌─────────────────────────────────┐
                              │ REPOSITORY: create(data)        │
                              │ INSERT INTO clientes            │
                              │ VALUES (...)                    │
                              │ RETURNING *  ──────┐            │
                              └────────────────────┼────────────┘
                                                   │
                                                   ▼
                              ┌─────────────────────────────────┐
                              │ SUPABASE                        │
                              │ Ejecuta SQL                     │
                              │ Retorna registro creado         │
                              └────────────┬────────────────────┘
                                           │ { id: "abc", ... }
┌──────────────────────────────────────────┼───────────────────────┐
│ 3. ACTION (continuación)                 │                       │
│      revalidatePath("/dashboard/clientes")                       │
│      return { success: true, data: cliente }                     │
│    } catch (error) {                                             │
│      return handleError(error) ──────────┐                       │
│    }                                     │                       │
└──────────────────────────────────────────┼───────────────────────┘
                                           │
                                           ▼
                              ┌─────────────────────────────────┐
                              │ ERROR HANDLER                   │
                              │ Si ValidationError:             │
                              │   return { success: false,      │
                              │            error: "...",        │
                              │            code: "..." }        │
                              └────────────┬────────────────────┘
                                           │ ActionResult
                                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. COMPONENTE: Recibe respuesta                                 │
│    if (result.success) {                                         │
│      toast.success("Cliente creado")                             │
│      router.refresh() // Actualiza lista                        │
│    } else {                                                      │
│      toast.error(result.error)                                   │
│    }                                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## Ejemplo: Validación en Cascada

```
createCliente({ nombre: "", email: "invalid" })
    │
    ▼
┌─────────────────────────────────────────┐
│ ClienteService.create()                 │
│ ┌─────────────────────────────────────┐ │
│ │ validateClienteData()               │ │
│ │ • nombre vacío ❌                   │ │
│ │ • email inválido ❌                 │ │
│ │                                     │ │
│ │ throw ValidationError(              │ │
│ │   "Errores de validación",          │ │
│ │   {                                 │ │
│ │     nombre: ["Requerido"],          │ │
│ │     email: ["Formato inválido"]     │ │
│ │   }                                 │ │
│ │ )                                   │ │
│ └─────────────────────────────────────┘ │
└────────────┬────────────────────────────┘
             │ Error lanzado
             ▼
┌─────────────────────────────────────────┐
│ Action catch block                      │
│ handleError(error)                      │
│   ↓                                     │
│ return {                                │
│   success: false,                       │
│   error: "Errores de validación",       │
│   code: "VALIDATION_ERROR"              │
│ }                                       │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│ UI muestra errores específicos          │
│ • Campo "Nombre": "Requerido"           │
│ • Campo "Email": "Formato inválido"     │
└─────────────────────────────────────────┘
```

---

## Separación de Responsabilidades (SOLID)

### ❌ ANTES (Violaciones SOLID)

```
┌──────────────────────────────────────────────────┐
│ createCliente(data)                              │
│                                                  │
│ 1. ❌ Autenticación (Responsabilidad 1)         │
│    const { user } = await supabase.auth...      │
│                                                  │
│ 2. ❌ Autorización (Responsabilidad 2)          │
│    if (!user) return error                      │
│                                                  │
│ 3. ❌ Validación (Responsabilidad 3)            │
│    if (!data.nombre) return error               │
│                                                  │
│ 4. ❌ Lógica de negocio (Responsabilidad 4)     │
│    if (es_menor && !responsable) ...            │
│                                                  │
│ 5. ❌ Acceso a BD (Responsabilidad 5)           │
│    await supabase.from("clientes").insert()     │
│                                                  │
│ 6. ❌ Cache (Responsabilidad 6)                 │
│    revalidatePath()                             │
│                                                  │
│ = 6 RESPONSABILIDADES EN 1 FUNCIÓN ❌           │
└──────────────────────────────────────────────────┘
```

### ✅ DESPUÉS (Siguiendo SOLID)

```
┌────────────────┐  ┌─────────────────┐  ┌────────────────┐
│ withAuth()     │  │ Service.create()│  │ Repo.create()  │
│ • Autenticación│  │ • Validación    │  │ • SQL Query    │
│ • Autorización │  │ • Reglas negocio│  │ • Error BD     │
└────────────────┘  └─────────────────┘  └────────────────┘
        │                   │                     │
        └───────────────────┼─────────────────────┘
                            │
                ┌───────────▼──────────┐
                │  createCliente()     │
                │  • Orquestación      │
                │  • Cache             │
                │  • Return result     │
                └──────────────────────┘

= 1 RESPONSABILIDAD POR COMPONENTE ✅
```

---

## Inyección de Dependencias

```
┌──────────────────────────────────────────────────┐
│ Testing con Mocks                                │
└──────────────────────────────────────────────────┘

// Producción
const realRepo = new ClienteRepository(supabase)
const service = new ClienteService(realRepo)
await service.create(data) → Llama a BD real

// Testing
const mockRepo = {
  create: jest.fn().mockResolvedValue({ id: "123" }),
  findByContact: jest.fn().mockResolvedValue(null)
}
const service = new ClienteService(mockRepo)
await service.create(data) → Llama a mock, NO a BD

✅ Servicio es testeable sin base de datos real
```

---

## Extensibilidad (Open/Closed Principle)

```
┌───────────────────────────────────────────────────┐
│ Agregar nuevo tipo de error: ✅ Sin modificar    │
│ código existente                                  │
└───────────────────────────────────────────────────┘

// Nuevo error personalizado
export class PaymentError extends AppError {
  constructor(message: string) {
    super(message, "PAYMENT_ERROR", 402)
  }
}

// handleError() automáticamente lo maneja
// NO requiere modificación ✅

┌───────────────────────────────────────────────────┐
│ Agregar nueva entidad: ✅ Patrón establecido     │
└───────────────────────────────────────────────────┘

1. Crear ProductoRepository extends BaseRepository
2. Crear ProductoService
3. Crear actions usando withAuth + service
4. ¡Listo! Sin tocar infraestructura existente
```

---

## Diagrama de Dependencias

```
app/actions/
    ↓ depende de
lib/auth/
lib/services/
    ↓ depende de
lib/repositories/
    ↓ depende de
@supabase/supabase-js

Cross-cutting:
lib/errors/  ← Usado por todos
lib/utils/   ← Usado por todos
```

**Regla**: Dependencias van solo hacia abajo  
**No permitido**: Repositorio NO puede importar Service  
**No permitido**: Auth NO puede importar Actions

---

## Matriz de Responsabilidades

| Capa | Autenticación | Validación | Lógica Negocio | Acceso BD | Cache |
|------|---------------|------------|----------------|-----------|-------|
| **Actions** | Delega a middleware | - | - | - | ✅ |
| **Middleware** | ✅ | - | - | - | - |
| **Services** | - | ✅ | ✅ | Delega a repo | - |
| **Repositories** | - | - | - | ✅ | - |

**Cada capa tiene UNA responsabilidad clara** ✅

---

## Archivos por Módulo

```
Módulo: Clientes
├── lib/types.ts ──────────────→ interface Cliente
├── lib/repositories/
│   └── cliente-repository.ts ─→ Data access
├── lib/services/
│   └── cliente-service.ts ────→ Business logic
└── app/actions/
    └── clientes.ts ───────────→ Orchestration

Total: 4 archivos por módulo completo
```

---

**Ver también:**
- [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) - Resumen de lo implementado
- [REFACTORING_GUIDE.md](REFACTORING_GUIDE.md) - Guía detallada
- [QUICK_REFERENCE.md](QUICK_REFERENCE.md) - Templates y snippets
